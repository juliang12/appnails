export interface DataProps{
    details: string
  credit: string
  manicure: string
  exit: number
  income: number
  id?: number
  }

  export interface Props{
    children?: React.ReactNode
  }